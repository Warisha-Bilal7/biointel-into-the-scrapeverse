Role: Architecture, Integration, Testing, Bright Data, and DeploymentGoal: Connect Warisha's backend to Arsh's frontend, configure the scraper, manage E2E testing, and build the final pitch assets.1. Bright Data Extraction (Hour 1–4)Action: Use Bright Data Scraper Studio on the target site Arsh selects.Targeting: Extract the exact DOM elements dictated by Arsh's domain definition.Delivery: Configure a Webhook delivery action to push the JSON to a temporary ngrok URL (provided by Warisha) or a RequestBin to verify the JSON shape.2. Cloud Deployment & Dockerization (Hour 5–8)Action: Containerize Warisha's FastAPI and PostgreSQL/pgvector environment using Docker Compose.Deployment: Deploy the backend to a cloud provider (e.g., Render, Railway, or AWS). Deploy Arsh's Next.js frontend to Vercel.Configuration: Update the Bright Data webhook to point to the live backend URL. Ensure backend CORS accepts the Vercel frontend URL.3. E2E Testing & Fallback Generation (Hour 9–10)Action: Run end-to-end testing. Trigger a manual scrape and watch it appear on the live Vercel site.The Safety Net: Write a mock_webhook_sender.py script. If the target website blocks the scraper right before judging, use this script to blast pre-saved JSON payloads to the backend so the live demo still works flawlessly.4. Architecture Diagrams & Pitch Prep (Hour 11+)Action: Create the UML files to include in the Devpost submission and README.Required Diagrams (use Draw.io or Mermaid):Component Diagram: Target Site $\rightarrow$ Bright Data $\rightarrow$ FastAPI $\rightarrow$ Postgres/pgvector (DB) & AI Engine $\rightarrow$ Next.js UI.Sequence Diagram: The lifecycle of a single data pull (Cron $\rightarrow$ Scrape $\rightarrow$ Drift Math $\rightarrow$ DB $\rightarrow$ Dashboard).State Machine: The states of a ScrapeEvent (e.g., Ingested $\rightarrow$ Analyzing $\rightarrow$ Validated OR Quarantined).
